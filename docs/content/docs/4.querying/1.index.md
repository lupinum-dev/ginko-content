---
title: Querying
description: Query collections with the unified content API.
---

The unified query API reads through collection handles exported from `content.config.ts`. Use the async functions (`one`, `many`, `resolveOne`, `variants`, `tree`, `neighbors`) in plain async code, and the `useContent*` composables when you want Nuxt async-data state.

## The shape

```ts
import { articles } from '~/content.config'

const latest = await many(articles, {
  where: { published: true },
  sort: { date: 'desc' },
  limit: 5
})
```

The options stay explicit:

- `where` for filters
- `$and`, `$or`, and `$not` for grouped logic
- `sort` for ordering
- `select(...fields)` for projection
- `limit` and `skip` for paging
- `by` for route, path, or ref lookup

Use `by: { path }` when you need an exact content path:

```ts
const page = await one(articles, {
  by: { path: '/articles/launch-notes' }
})
```

## Route-backed pages

For content-backed routes, use the page helper:

```ts
import { docs } from '~/content.config'

const { page } = await useContentPage(docs)
```

That keeps route-keyed loading, stale-result masking, locale variants, and content-route registration on the package side instead of rebuilding them in app code.

## Result helpers

| Method | Result |
|---|---|
| `many(handle, options)` | `T[]` |
| `one(handle, options)` | `T \| null` |
| `resolveOne(handle, options)` | `{ doc, explain }` |
| `tree(handle, options)` | navigation tree |
| `neighbors(handle, options)` | previous/next entries |

## What's next

- [Filters](/docs/querying/filters) — limit results with explicit clauses
- [Sorting and paging](/docs/querying/sorting-paging) — order and page lists
- [Projection](/docs/querying/projection) — return only the fields you need
