---
title: Content config
description: The full content.config.ts reference for collections and content-model parsing.
---

`content.config.ts` is the source of truth for typed collections and content-model parsing. Runtime behavior such as locale resolution, markdown rendering, and sitemap registration lives in `nuxt.config.ts` under `content`.

## Location

Place `content.config.ts` at the project root:

```ts [content.config.ts]
import { defineContentConfig } from '@lupinum/ginko-content/config'

export default defineContentConfig({
  // ...
})
```

## Top-level shape

```ts
defineContentConfig({
  collections: Record<string, Collection>
})
```

`collections` is required. Public querying, page resolution, navigation, search, and sitemap generation all start from named collections.
Use local `const` handles when a collection is only referenced inside `content.config.ts`; use `export const` for handles that pages, components, or server routes import.

## collections

Declare typed collections with [`defineCollection`](/docs/collections/define-collection):

```ts [content.config.ts]
import { defineCollection, defineContentConfig, fields } from '@lupinum/ginko-content/config'
import { z } from 'zod'

const articles = defineCollection('articles', {
  type: 'page',
  source: 'articles/**/*.md',
  strict: true,
  schema: z.object({ title: fields.text().required(), date: fields.date().required() })
})

const authors = defineCollection('authors', {
  type: 'data',
  source: 'authors/**/*.md',
  i18n: true,
  schema: z.object({ name: fields.text().required() })
})

export default defineContentConfig({
  collections: { articles, authors }
})
```

## Collection i18n

Collections can opt into locale-aware behavior directly:

```ts [content.config.ts]
import { defineCollection, defineContentConfig } from '@lupinum/ginko-content/config'

const docs = defineCollection('docs', {
  type: 'page',
  source: 'docs/**/*.md',
  i18n: true
})

const legal = defineCollection('legal', {
  type: 'page',
  source: 'legal/**/*.md',
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'de']
  }
})

export default defineContentConfig({
  collections: { docs, legal }
})
```

Global locale detection, fallback, and translated slug handling live in `nuxt.config.ts` under `content.i18n`.

## Putting it together

```ts [content.config.ts]
import { defineCollection, defineContentConfig, fields } from '@lupinum/ginko-content/config'
import { z } from 'zod'

const authors = defineCollection('authors', {
  type: 'data',
  source: 'authors/**/*.md',
  i18n: true,
  schema: z.object({
    name: fields.text().required(),
    avatar: fields.object({
      src: fields.image().required(),
      alt: fields.text()
    })
  })
})

const articles = defineCollection('articles', {
  type: 'page',
  source: 'articles/**/*.md',
  strict: true,
  schema: z.object({
    title: fields.text().required(),
    description: fields.textarea().required(),
    date: fields.date().required(),
    tags: z.array(z.string()).default([]),
    author: fields.relation('authors').required()
  })
})

export default defineContentConfig({
  collections: { authors, articles }
})
```

## What's next

- [Querying](/docs/querying) — use the schemas you just declared
- [Module options](/docs/api-reference/module-options) — runtime i18n, markdown, and sitemap behavior
- [API reference](/docs/api-reference/content-config) — every exported helper
