# I18n

Use this when the Nuxt app serves localized content routes.

## Required shape

- `@nuxtjs/i18n` is installed and registered.
- `content.i18n` declares `defaultLocale` and `locales`.
- Localized collections use `i18n: true`.
- Files live under `content/<locale>/...`.
- List links use `path` from `useContentMany()`.
- Raw query filters use `_path`.
- Route/page/navigation/search payloads use `path`.
- Nuxt Sitemap i18n output is validated through `/sitemap_index.xml`.

## Nuxt config

```ts
export default defineNuxtConfig({
  modules: [
    '@nuxtjs/i18n',
    '@lupinum/ginko-content'
  ],
  i18n: {
    defaultLocale: 'en',
    strategy: 'prefix_except_default',
    locales: [
      { code: 'en', language: 'en-US', name: 'English' },
      { code: 'de', language: 'de-DE', name: 'Deutsch' }
    ]
  },
  content: {
    i18n: {
      defaultLocale: 'en',
      locales: ['en', 'de'],
      fallback: {
        de: ['en']
      }
    }
  }
})
```

`i18n.locales[].code` and `content.i18n.locales` must use the same locale codes.

## Content config

```ts
import { defineCollection, defineContentConfig } from '@lupinum/ginko-content/config'
import { z } from 'zod'

export default defineContentConfig({
  collections: {
    docs: defineCollection({
      type: 'page',
      source: 'docs/**/*.md',
      i18n: true,
      schema: z.object({
        title: z.string(),
        description: z.string().optional()
      })
    })
  }
})
```

Do not include locale folders in `source`.

## Content tree

```txt
content/
  en/
    docs/
      index.md
      getting-started.md
  de/
    docs/
      index.md
      getting-started.md
```

## Common failure

Do not build links like this:

```ts
`/${locale}${post._path}`
```

Use `useContentMany()` and bind `post.path`:

```vue
<NuxtLink :to="post.path">
  {{ post.title }}
</NuxtLink>
```

## Validation

```bash
pnpm build
npx ginko-content doctor --i18n
rg "/de/de/|/fr/fr/" .output/public
```

Expected sitemap output for Nuxt Sitemap's default i18n mode:

```txt
.output/public/sitemap_index.xml
.output/public/__sitemap__/en-US.xml
.output/public/__sitemap__/de-DE.xml
```

Submit `/sitemap_index.xml`. Do not add route rules, prerender ignores, or `sitemap.autoI18n: false` only to make `/sitemap.xml` a physical XML file.
